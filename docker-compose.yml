version: '3'
services:
  web:
    build: .
    ports:
    - "5000:5000"
    volumes:
    - .:/code
    - logvolume01:/var/log
    links:
    - redis
    command: python manage.py runserver 0.0.0.0:5000
  redis:
    image: redis
volumes:
  logvolume01: {}